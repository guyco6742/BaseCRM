# Local development points at the DEV Supabase project (TheGuysCRM-dev).
# Production (Netlify) sets these same vars to the PROD project in the Netlify UI.
#
#   DEV project:  TheGuysCRM-dev  (atgoyrojmwxmntonbmpd)  <- local .env uses this
#   PROD project: TheGuysCRM      (iezgyetfwgmlczcrnrvx)  <- Netlify env vars use this
#
# העתיקו קובץ זה ל-.env ומלאו את הפרטים של פרויקט ה-DEV (Supabase → Project Settings → API)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
