-- ============================================================================
-- מיגרציה 016 — עמודת last_sent_at בטבלת invitations, לתמיכה בשליחת מייל הזמנה
--                אמיתי (Resend) דרך פונקציית ה-edge החדשה send-invite.
-- הריצו ב-Supabase → SQL Editor (בטוח להריץ שוב, idempotent).
--
-- הבעיה: הזמנות נוצרות היום רק כשורת DB, והמשתמש המזמין צריך להעתיק ולשלוח
-- קישור ידנית. בנוסף, אין דרך לדעת מתי נשלחה הזמנה בפועל — נדרש כדי לחסום
-- לחיצות חוזרות מהירות על "שלח שוב".
--
-- הפתרון: עמודת last_sent_at שמתעדכנת בכל שליחת מייל מוצלחת (ראו §7 Item 4
-- ב-docs/superpowers/specs/2026-07-08-remediation-prd-and-tech-spec.md).
-- ============================================================================

alter table public.invitations add column if not exists last_sent_at timestamptz;
